import {AppBar} from '@mui/material';

const Navbar = () => {
    return (
        <AppBar position="static" color="transparent">
         <h1>NavBar</h1>
            
        </AppBar>
       
    )
}

export default Navbar;